function [out1,out2]=mybutter(freq,order)% sys=mybutter(freq,order)%   returns the transfer function for a Butterworth filter%   unity gain, knee at 'freq', with 'order' poles%   Brian Lantz%   if called with two output args, it returns the numerator and denominator of a TF%  [num,den]=mybutter(freq,order)% modified for sys on Jan 8 2001 BTL[butZ, butP, K]=buttap(order);butPscaled= 2*pi * freq*butP;gain=(2*pi*freq)^order;if nargout==1	out1=zpk(butZ,butPscaled, K*gain);else	[out1, out2]=zp2tf(butZ,butPscaled, K*gain);end